# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
# Typically, you should not commit the node_modules directory.
# These packages are installed by running `npm install` or `yarn install`.
/node_modules

# Build directory
# This directory contains the optimized production build of your app.
# It's generated by running `npm run build` or `yarn build`.
/build

# Environment variables
# CRITICAL: Never commit your environment files. They contain sensitive API keys.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Production
# The .DS_Store file is a metadata file created by macOS's Finder.
.DS_Store

# Miscellaneous
# Logs and temporary files that might be generated during development.
npm-debug.log*
yarn-debug.log*
yarn-error.log*
